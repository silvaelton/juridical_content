= simple_form_for @article_category do |f|
  .field
    = f.input :name, input_html: { placeholder: "Digite o título do artigo"}
  .fields
    .field.wide.five
      = f.input :father_id, as: :select, collection: Internal::ArticleCategory.all.order(:name), include_blank: false, include_blank: "Nenhuma"
    .field.wide.two
      = f.input :enable, as: :select, include_blank: false
    .field.wide.two
      = f.input :portal_view, as: :select, include_blank: false
    .field.wide.two
      = f.input :only_user, as: :select, include_blank: false
  .field
    = f.button :submit, data: { disable_with: t(:disable_with) }
    = link_to t(:back_button), article_categories_path, class: 'ui button', data: { disable_with: t(:disable_with) }